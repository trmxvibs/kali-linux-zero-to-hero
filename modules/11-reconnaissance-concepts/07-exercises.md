# Exercises — Module 11

1. Run `nmap -sV -T2` against your Metasploitable VM and list every open port with its version.
2. For the FTP service reported, determine (via research only, no exploitation) why its specific version might be considered high-risk. You'll test this properly, safely, in Module 20.
3. Explain the difference between `STATE: closed` and `STATE: filtered` and what each implies about a firewall's presence.
