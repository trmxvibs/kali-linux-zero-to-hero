# Challenge — Module 03

Write a one-line command (no script file) that:
- Prints your username, current directory, and current date on one line
- Uses at least one environment variable and one command substitution
- Uses double quotes correctly so that everything expands as intended

Example shape (don't just copy this — build your own): `echo "..."` combining `$USER`, `$(pwd)`, and `$(date)`.
