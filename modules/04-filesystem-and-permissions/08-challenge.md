# Challenge — Module 04

Without looking anything up first, predict the exact `ls -l` output string for a file with permissions octal `640`, with SGID also set. Then create the file and verify your prediction with `chmod`, `ls -l`, and `stat -c "%a"`. If your prediction was wrong, explain exactly which part you got wrong and why.
