# Exercises — Module 03

1. Add a permanent alias `ll='ls -la'` to your `~/.bashrc`, then open a new terminal and confirm it persists.
2. Explain, in your own words, why `./script.sh` is required instead of just `script.sh`, even when the script is executable and in your current directory.
3. Use `man -k` to find three commands related to "process" that you haven't used yet in this course.
